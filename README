A collection of scripts that may or may not be useful in the future.

DosBoot is a perl script meant to detect and block the sources of a DoS attack.

setup_home.sh will create a symlink in your home directory pointing to each file
in the specified directory (defaults to ~/homeconfig). This is meant to ease setup
of dotfiles which are stored in a git repository.
